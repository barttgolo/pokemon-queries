import { Pokemon } from "@/components/pokemon";

export const App = () => {
  return (
    <div className="w-screen min-h-screen bg-slate-300 flex items-center justify-center">
      <Pokemon />
    </div>
  );
};
